<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/action_bar</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomCheckboxTheme" parent="@android:style/Widget.CompoundButton.Star">
        <item name="android:background">@drawable/mark_bg</item>
    </style>

    <style name="Divider">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">@dimen/divider_height</item>
        <item name="android:layout_marginLeft">@dimen/tv_margin_left</item>
        <item name="android:layout_marginStart" tools:ignore="NewApi">@dimen/tv_margin_left</item>
        <item name="android:layout_marginRight">@dimen/tv_margin_right</item>
        <item name="android:layout_marginEnd" tools:ignore="NewApi">@dimen/tv_margin_right</item>
        <item name="android:layout_marginTop">@dimen/margin_top_16</item>
        <item name="android:background">@color/divider_color</item>
    </style>
</resources>
